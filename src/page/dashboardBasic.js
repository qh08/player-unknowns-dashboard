import React from 'react';
import PropTypes from 'prop-types';

const dashboardBasic = props => {
    return (
        <div>
            b
        </div>
    );
};

dashboardBasic.propTypes = {
    
};

export default dashboardBasic;