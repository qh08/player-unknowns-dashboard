import React from 'react';
import PropTypes from 'prop-types';

const dashboardAdvanced = props => {
    return (
        <div>
            a
        </div>
    );
};

dashboardAdvanced.propTypes = {
    
};

export default dashboardAdvanced;