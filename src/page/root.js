import React from 'react';
import PropTypes from 'prop-types';

const root = props => {
    return (
        <div>
            123
        </div>
    );
};

root.propTypes = {
    
};

export default root;