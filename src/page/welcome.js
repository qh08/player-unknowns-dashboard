import React from 'react';
import PropTypes from 'prop-types';

const welcome = props => {
    return (
        <div>
            welcome
        </div>
    );
};

welcome.propTypes = {
    
};

export default welcome;